# Icons

All icons are monochrome by default. On hover, their true color is revealed. Pass in optional background and foreground fill to override the default monochrome color (text-secondary). See Rust.tsx for reference.
